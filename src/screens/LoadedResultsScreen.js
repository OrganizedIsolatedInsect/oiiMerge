import React from 'react';
import {View, Text} from 'react-native';

import styles from '../constants/Styles';

const LoadedResultsScreen = props => {
  return (
    <View>
      <Text>Loaded Results Screen</Text>
    </View>
  );
};

export default LoadedResultsScreen;
